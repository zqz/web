version: 2
jobs:
  build:
    docker:
      - image: dylanj/circleci-go:latest 

    working_directory: /go/src/github.com/zqz/upl
    steps:
      - checkout

      - run: go get -v -t -d ./...
      - run: go test -v ./filedb
